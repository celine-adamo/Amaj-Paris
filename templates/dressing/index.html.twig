{% extends 'base.html.twig' %}

{% block title %}
{% trans %}Dressing Unisexe{% endtrans %}
{% endblock %}

{% block body %}
  <div class="container contner">
    <div class="row">
      <h2 id="iconics-title" class="text-center mt-3">
        {% trans %}Le Dressing Unisexe{% endtrans %}
      </h2>
      <div class="col-md-4 offset-md-4 align-items-center text-center p-2">
          <h3 id="iconics-title" class="text-nowrap mt-3 text-white bg-dark">
        {% trans %}Je choisis mon produit{% endtrans %}
      </h3></div>
      <div class="d-flex justify-content-around row">
        {% for product in products %}
          <div class="col-6 col-md-3 my-5">
            <a class="nav-link text-dark" href="{{
              path(
                'one-dressing',
                {
                  slug: product.slug
                }
              )
              }}">
              <div class="card-body">
                <h5 class="card-title text-center">
                  {{ product.name }}
                </h5>
              </div>
              <img class="card-img-top clothe-shadow"
                src="{{ asset('assets/images/products') }}/{{ product.img }}"
                alt="Modèle {{ product.name }}" />
            </a>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
{% endblock %}
