{% extends 'base.html.twig' %}

{% block title %}S'inscrire
{% endblock %}

{% block body %}
{# 
	{{form(form)}} #}

		<div class="container">
			<div class="row">

				{{form_start(form)}}
				<div class="col-12">
					<div class="col-6">
						{{form_row(form.firstname)}}
					</div>
					<div class="col-6">
						{{form_row(form.lastname)}}
					</div>
				</div>

				{{form_end(form)}}
			</div>
		</div>
	{% endblock %}
